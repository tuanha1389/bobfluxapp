"use strict";
var f = require("./flux");
exports.createUserInfo = f.createComponent({
    render: function (ctx, me) {
        me.children = [
            { "tag": "h3", children: ctx.state.email, style: { padding: 5, margin: 0, textAlign: "right" } }
        ];
    }
});
