import * as f from "../flux";
export interface IUserInfo extends f.IComponentState {
    email: string;
    name: string;
    surName: string;
}
export declare const createDefaultUserInfo: () => IUserInfo;
export interface IUserAccountPageState extends f.IRouteComponentState {
    isEditingEnabled: boolean;
    editedUserInfo: IUserInfo | null;
}
export declare const createDefaultUserAccountPageState: () => IUserAccountPageState;
