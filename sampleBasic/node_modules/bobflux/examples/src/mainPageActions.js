"use strict";
var b = require("bobril");
var f = require('./flux');
var c = require('./states.cursors');
exports.goTo = f.createAction(c.rootCursor, function (state, routeKey) {
    b.runTransition(b.createRedirectReplace(routeKey));
    return f.shallowCopy(state);
});
