"use strict";
function __export(m) {
    for (var p in m) if (!exports.hasOwnProperty(p)) exports[p] = m[p];
}
var f = require("../../index");
__export(require("../../index"));
exports.createUpdateAction = function (cursor, postHandler) {
    return f.createAction(cursor, function (state, params) {
        return postHandler
            ? postHandler(f.shallowCopy(state, function (l) {
                Object.keys(params).forEach(function (p) {
                    l[p] = params[p];
                });
            }), params)
            : f.shallowCopy(state, function (l) {
                Object.keys(params).forEach(function (p) {
                    l[p] = params[p];
                });
            });
    });
};
